## Packages
react-qrcode-logo | Generate QR codes for sharing trip links
date-fns | Date formatting and manipulation
framer-motion | Smooth animations for lists and transitions
lucide-react | Icon library (already in base, but listing for completeness)
clsx | Class name utility (already in base)
tailwind-merge | Class merging (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'DM Sans'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"],
}
